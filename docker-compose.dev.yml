version: "3.9"

services:
  django-web:
    container_name: brewnest-backend-dev
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
      - ./static:/app/staticfiles
    ports:
      - "8000:8000"
    env_file:
      - .env.local
